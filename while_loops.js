// while loops are used to keep running as long as a condition is true
let count = 1;

while (count <= 5) {
  console.log("Count is:", count);
  count++; // Don't forget to update the variable!
}

// printing even numbers
let num = 2;

while (num <= 10) {
  console.log(num);
  num += 2;
}
