const variableName = value;
// now the value of variablename is constant
// and now it cannot be changed
const variableName3 = 'constantValue';
// This will throw an error because variableName is already declared
// variableName = 'newValue'; // Uncommenting this line will cause an error
