// yernary operator is used as a shortcut for if else operator
let age = 18;

let result = (age >= 18) ? "Adult" : "Minor";
console.log(result);


// Login status
let isLoggedIn = true;
let status = isLoggedIn ? "Welcome back!" : "Please log in.";
console.log(status);
